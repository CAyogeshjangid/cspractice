# M7 — Hardening + pilot readiness: complete to the limits of this environment

Date: 11 July 2026
Suite: **111 passed** (104 → 111). Ruff clean.

## Pre-launch checklist — live status

| Checklist item (charter M7) | Status |
|---|---|
| CORS explicit origins in prod compose | ✅ env-driven, wildcard refused at startup (tested since M1) |
| CSRF active, integration-tested | ✅ permanent security suite |
| Zero seeded/default credentials | ✅ none exist; gitleaks in CI |
| Redis-backed rate limiting verified across two api replicas | ✅ 429-on-exhaustion tested; replicas share the Redis window by construction; prod compose runs `api` ×2 — verify once on staging |
| CSP without unsafe-eval; headers on /api routes | ✅ tested |
| Cross-tenant test suite green | ✅ permanent suite |
| activity_log has no UPDATE/DELETE grant (verified by test) | ✅ `test_prelaunch.py` bootstraps the app role + grants and proves it live (also rule_version immutability) |
| .env.example complete; VERSION single-sourced | ✅ automated: test compares Settings fields to .env.example |
| HTTPS terminated in prod compose | ✅ Caddy overlay (`docker-compose.prod.yml` + `Caddyfile`) — needs a real domain on staging |
| Postgres automated backup + tested restore | ✅ `scripts/backup.sh` (dump + `pg_restore --list` verification + retention) and `scripts/restore.sh` (fresh-DB-only) — **executed live in this session**: 20 tables restored, overwrite refused |
| ZAP scan clean of high/medium | ⬜ BLOCKED here (no scanner egress) — run `zap-baseline.py -t https://staging...` on staging before pilot |
| All doc templates carry current validation stamps | ⬜ BY DESIGN pending: templates ship unstamped until the named professional stamps them (PRD §4.7) |

Also delivered this milestone:
- **Live arq worker test** (M5 carryover): real Redis enqueue → real Worker
  (burst mode) → dispatch sent; only the SMTP socket is stubbed.
- **Calendar Word export** (M4 carryover): per-entity .docx with rule
  code/version/citation per row and a generation stamp (export fidelity §10).
- **Load sanity**: 100 concurrent calendar reads, all 200 with correct data.
- **C12 sweep test**: no date arithmetic in routes/.
- `docs/RUNBOOK.md`: deploy, backup/restore, secret rotation (incl. the
  Fernet/JWT_SECRET coupling), dead-letter handling, the 48-hour circular
  drill, cross-tenant incident response.

## Remaining before pilot invite (the honest gate)

1. **Staging deploy** on a real host: `docker compose -f docker-compose.yml
   -f docker-compose.prod.yml up -d` with a domain; verify two-replica rate
   limiting and run the ZAP baseline there. (~half a day)
2. **Playwright E2E** — blocked on the frontend build-out: the React app
   currently covers login/register/companies only. The full charter flow
   (invite→import→calendar→remind→generate) needs the UI pass first. All
   flows are integration-tested at the API layer today.
3. **The rules dataset** — the product-blocking item that is not code:
   Open Question #3 (named professional owner) must be resolved, the ROC/IT/
   GST dataset authored and signed, loaded via `python -m app.rules.load`.
4. **Template stamping** by the reviewing professional (with whatever layout
   polish they require — polish and stamping are one review pass).
5. mypy --strict adoption (ruff enforced today) — quality debt, not a gate.
